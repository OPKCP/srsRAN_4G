# Трекинг-файл: Переход на Open5GS ради рабочего S1-хендовера

Ветка: `feat/handover-open5gs-migration`

## Цель

Обеспечить рабочий **S1-хендовер** между двумя базовыми станциями стенда (БС1 `base5` и
БС2 `pi5-2`), заменив текущее ядро сети `srsepc` на **Open5GS** — сторонний EPC с
поддержкой S1-хендовера (официально заявлено: "Handover (5GC Xn/N2 and EPC S1/X2)").

## Почему это решение (краткая обоснованность)

- В `srsepc` (и апстрим-`srsRAN_4G`, и нашем форке) **MME не обрабатывает S1AP `HandoverRequired`**
  — в `srsepc/src/mme/s1ap.cc` ветки `ho_required` нет, для него выводится
  `"Unhandled S1AP initiating message"`. Следовательно, полноценный S1-хендовер в `srsepc`
  end-to-end не работает.
- Официальная документация srsRAN 4G прямо указывает: *"To support S1 handover, a third party
  EPC must be used. We will use Open5GS..."*.
- Open5GS — открытый, активно поддерживаемый EPC/5GC, есть рабочие примеры S1-хендовера
  с srsENB (AERPAW SE5, `szklisahub/s1handover-srsran-open5gs`).
- X2-хендовер в нашем `srsenb` НЕ доступен: `srsenb/hdr/x2_adapter.h` — **dummy X2AP**.
  Поэтому реально используем именно S1-хендовер (через MME), который Open5GS поддерживает.

## Текущая архитектура стенда (исходные данные)

> ⚠️ **Актуально на 2026-09-07: pi4 вышло из строя.** Ядро сети решено разворачивать
> на **pi5-2**. Топология ниже отражает это изменение.

| Узел | IP | Роль | SSH |
|------|----|------|-----|
| ~~pi4~~ | ~~192.168.1.27~~ | ~~Ядро EPC~~ — **вышло из строя** | — |
| **pi5-2** (eNB2 / будущий EPC) | 192.168.1.26 | БС №2 (1811 МГц, EARFCN 1260, pci 5) **+ новое ядро Open5GS** | `ssh pi5` (`infarh`) |
| **Base5** (eNB1) | 192.168.1.25 | БС №1 (1815 МГц, EARFCN 1300, pci 1) | `ssh base5` (`shmac`) |

**Новая целевая схема (ядро на pi5-2):**
- Open5GS разворачивается на **pi5-2 (192.168.1.26)**; S1-MME/SCTP 36412, GTP-U 2152 слушаются на пи5-2.
- Обе БС (Base5 и pi5-2) подключаются к ядру по **192.168.1.26**.
- Абонентская подсеть 10.0.0.0/24 (шлюз 10.0.0.1, SGi).
- MCC/MNC — уточнить (в `epc.conf` 250/01, в `prod/readme.md` 250/63); APN IS2026.net (в epc.conf может быть internet.mts.ru), TAC 0x0007, LAC 0x0006.
- SIP (Asterisk) 10.0.0.1:5060, веб-раздача 10.0.0.1:8080 — переносятся/пересоздаются на pi5-2.

## Текущие компоненты, затрагиваемые миграцией

| Компонент | Что делает | Что меняется при переходе на Open5GS |
|-----------|-----------|---------------------------------------|
| `srsepc` (контейнер `epc` на pi4) | MME/HSS/SPGW | Полностью заменяется Open5GS |
| `srsran-dashboard` | парсит `epc.log` (srsepc) + `user_db.csv` | Нужна адаптация под лог Open5GS и его БД |
| `user_db.csv` | БД абонентов (IMSI, Key, OPc, QCI, APN) | Переносится в БД Open5GS (WebUI/CLI) |
| Скрипты `experiment/run_epc_pi5-2.sh` | запуск контейнера EPC | Переписывается под образ Open5GS |
| Веб-сервер / SIP на pi4 | 10.0.0.1:8080 / 5060 | IP-пул и шлюз SGi надо согласовать с Open5GS |

---

## Легенда статусов

- ❌ Не начат
- 🔄 В процессе
- ✅ Завершён
- ⚠️ Требует правок / есть проблема

---

## План работ

### Фаза 0. Подготовка и контекст
- [x] ✅ Зафиксировать актуальную конфигурацию srsepc (epc.conf) и user_db.csv стенда (см. ниже).
- [ ] ❌ Уточнить версию/архитектуру pi4 (ARM64) для выбора образа Open5GS.
- [x] ✅ Зафиксировать формат абонентских записей (IMSI, Key, OPc, AMF, SQN, QCI, APN) для переноса.

### Актуальные данные стенда (изучено 2026-09-07)

- ММЕ/EPC на **pi4 (192.168.1.27)**: контейнер `epc`, S1-MME/SCTP 36412, GTP-U/2152,
  абонентская подсеть **10.0.0.0/24 (шлюз 10.0.0.1, SGi)**, NAT/MASQUERADE.
- MCC/MNC **250/01** (по IMSI 25001...), APN **IS2026.net** (из `srsran_configs/prod/readme.md`),
  TAC 0x0007, LAC 0x0006, DNS 8.8.8.8, шифрование EEA0 / целостность EIA1.
- **Формат `user_db.csv`** (HSS srsEPC): `Name,Auth,IMSI,Key,OP_Type,OP/OPc,AMF,SQN,QCI,IP_alloc`.
  Примеры: `250010000000001`, `250010000000002` (MILENAGE, opc, QCI 9, dynamic IP).
- **Дашборд**: парсер `parser/epc_parser.py` завязан на формат лога **srsepc**
  (S1 Setup Request, `Attach request -- IMSI`, `IMSI: ..., UE IP: ...`, `User not found`, `Added user from DB`,
  `static ip addr`). Для Open5GS потребуется адаптация под его лог/API.
- **Скрипты запуска**: EPC запускается на pi4 из `~/srsran/scripts/run_epc_pi5-2.sh`
  (в репозитории версия с устаревшими адресами 192.168.1.16/20; на малинке актуальные 192.168.1.27).
  NAT/форвардинг для 10.0.0.0/24 настраивается внутри контейнера `epc` (network host).
- **X2 в srsenb НЕ работает** (`srsenb/hdr/x2_adapter.h` — dummy X2AP) → используем только S1-хендовер.

### Фаза 1. Развёртывание Open5GS на pi5-2 (ядро переносится с pi4)
- [ ] ❌ Выбрать способ развёртывания: Docker-образ `open5gs/open5gs` (официальный, multi-arch ARM) или сборка на ARM64.
- [x] ✅ Создан задел в каталоге `open5gs/`: docker-compose, шаблоны конфигов (mme/sgwu/pgwu), скрипт миграции БД, README.
- [ ] ❌ Подготовить конфиг Open5GS: MME (S1-MME 36412), SGW (GTP-U 2152), SGi (10.0.0.1/24), PLMN (250/01 или 250/63 — уточнить), TAC, APN, DNS.
- [ ] ❌ Настроить SCTP/порты так, чтобы БС1 (Base5, 192.168.1.25) и БС2 подключались к пи5-2 (192.168.1.26).
- [ ] ❌ Проверить подключение обеих БС по S1AP (S1 Setup) в логах Open5GS MME.

### Фаза 2. Перенос базы абонентов (user_db.csv → Open5GS)
- [x] ✅ Написан и проверен скрипт `open5gs/migrate_userdb_to_open5gs.py` (разбор `user_db.csv` → JSON Open5GS; тест на реальном файле: 2 абонента).
- [ ] ❌ Собрать актуальные записи из `srsran_configs/user_db.csv` (и `srsran_configs/prod/...` при наличии) на pi5-2.
- [ ] ❌ Загрузить субскрипции в Open5GS (WebUI или через MongoDB/CLI) по выводу скрипта.
- [ ] ❌ Согласовать статический/динамический IP-пул с подсетью 10.0.0.0/24 (шлюз 10.0.0.1).
- [ ] ❌ Проверить attach реального/`srsue` абонента и выдачу IP.

### Фаза 3. Адаптация дашборда srsran-dashboard под Open5GS
- [ ] ❌ Изучить формат логов Open5GS MME (S1 Setup, Attach, детач, ошибки аутентификации).
- [ ] ❌ Адаптировать `parser/epc_parser.py`: распознавание событий из лога Open5GS вместо srsepc.
- [ ] ❌ Решить вопрос с `user_db.csv` (Open5GS не использует его; нужен режим чтения своей БД или список через API Open5GS).
- [ ] ❌ Обновить документацию дашборда (README) и переменные окружения (DASH_EPC_LOG и т.п.).
- [ ] ❌ Проверить работу дашборда в режиме `epc` на реальных логах Open5GS.

### Фаза 4. Проверка S1-хендовера end-to-end
- [ ] ❌ Настроить соседство/измерения (A3) между БС1 и БС2 (rr.conf / sib.conf) как требуется.
- [ ] ❌ Проверить HandoverRequired от БС1 → MME Open5GS → HandoverRequest на БС2 → HandoverNotify.
- [ ] ❌ Проверить непрерывность сеанса/интернета (срыв пакетов минимальный) при хендовере.
- [ ] ❌ Зафиксировать результаты, логи и PCAP (по возможности) в `docs/`.

### Фаза 5. Документация и доводка
- [ ] ❌ Обновить `AGENTS.md` / `srs-readme.md` — новая топология ядра Open5GS.
- [ ] ❌ Обновить скрипты запуска EPC (`experiment/`) под Open5GS.
- [ ] ❌ Обновить/добавить описание в `srsran-dashboard/README.md` и `docs/`.
- [ ] ❌ Коммит всех изменений в ветке `feat/handover-open5gs-migration`.

---

## Открытые вопросы / риски

1. **Совместимость srsENB с Open5GS** в части криптографии: Open5GS сам ведёт NH/KeNB* для
   S1-хендовера — проверить, что srsENB корректно принимает `HandoverCommand` (target-to-source
   transparent container).
2. **NEA/iA и алгоритмы шифрования**: текущий стенд использует EEA0/EIA1; Open5GS по умолчанию
   поддерживает широкий набор — убедиться, что согласование проходит с `srsue`.
3. **Двойная работа srsepc и Open5GS** на pi4: для отката можно временно держать оба ядра на разных
   портах/сетях, но S1-MME/SCTP 36412 и GTP-U 2152 монопольны — нужен осознанный выбор.
4. **Архитектура pi4 (ARM64)**: образ Open5GS должен быть под эту архитектуру (официальные
   multi-arch образы есть).
5. **Дашборд и правки ядра**: дашборд завязан на `user_db.csv` — после миграции требуется
   отдельное решение (чтение БД Open5GS через его API или собственный экспорт).

---

## Решения по ходу работы (журнал)

| Дата | Решение | Обоснование |
|------|---------|-------------|
| 2026-09-07 | Создана ветка `feat/handover-open5gs-migration` и этот трекинг-файл | Новая работа по хендоверу изолируется от `master` |
| 2026-09-07 | Ядро сети переносится на **pi5-2** (192.168.1.26) из-за отказа pi4; Open5GS разворачивается на pi5-2 | pi4 вышло из строя; pi5-2 уже есть в топологии как БС2 и может принять роль EPC |
| 2026-09-07 | Создан каталог `open5gs/` + проверенный скрипт миграции `migrate_userdb_to_open5gs.py` | Конкретный задел к Фазам 1–2 (развёртывание и перенос БД) |
| 2026-09-07 | APN/PLMN уточняются при развёртывании (в epc.conf: 250/01, internet.mts.ru; в prod/readme.md: 250/63, IS2026.net) | Источники конфигурации противоречат друг другу; фактическая рабочая конфигурация — на малинках |
