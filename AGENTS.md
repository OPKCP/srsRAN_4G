# AGENTS.md — Информация для агентов (Copilot / кодинг-агенты)

Этот файл помогает быстро вникнуть в проект **srsRAN_4G** (форк srsRAN / srsLTE)
— стенд-демонстратор LTE-сети на оборудовании Raspberry Pi + Software Defined Radio
(USRP). Ниже — краткая карта проекта, ключевые файлы, документация и практики.

## Что это

Локальная пико-сота LTE (4G) на базе:
- **srsenb** — базовая станция (eNB), работающая на SDR (USRP B210 / NI USRP-2901).
- **srsepc** — ядро сети (EPC: MME/HSS/SPGW) — управляющая и пользовательская плоскость.
- **srsue** — абонентское устройство (реже).
- Дистрибутив поставляется как Docker-образ `ghcr.io/opkcp/srsran_4g/srsran_4g:ARM`.

## Текущая сетевая топология стенда (актуально на 2026-09-04)

| Узел | IP | Роль | SSH |
|------|----|------|-----|
| **pi4** | 192.168.1.27 | Ядро EPC + веб-сервер + SIP + дашборд ядра | `ssh pi4` (пользователь `shmac`) |
| **Base5** (eNB1) | 192.168.1.25 | Базовая станция №1 | `ssh base5` (`shmac`) |
| **pi5-2** (eNB2) | 192.168.1.26 | Базовая станция №2 | `ssh pi5` (`infarh`) |

- БС1: **1815 МГц** (EARFCN 1300, pci 1, eNB id 0x19b) — USRP B210 (sn 000001406).
- БС2: **1811 МГц** (EARFCN 1260, pci 5, eNB id 0x19f) — USRP-2901 (sn 3150E8C).
- Обе БС подключаются к ядру по **192.168.1.27** (S1-MME/SCTP 36412, GTP-U 2152).
- Абонентская подсеть: 10.0.0.0/24 (шлюз 10.0.0.1, SGi). SIP (Asterisk) 10.0.0.1:5060,
  веб-раздача файлов 10.0.0.1:8080.

> ⚠️ Глава 1 `docs/STARTUP_SEQUENCE.md` содержит СТАРЫЕ адреса (192.168.1.16/15) —
> актуальная топология выше и в `srs-readme.md`.

## Ключевые каталоги / пути

- `experiment/` — скрипты запуска контейнеров стенда (единый источник, переносятся на малинки):
  - `run_epc_pi5-2.sh`, `run_enb_rpi.sh`, `run_enb2_pi5-2.sh`,
    `run_webserver_pi5-2.sh`, `asterisk/run_asterisk_pi5-2.sh`.
- `srsran-dashboard/` — веб-дашборд (Flask) мониторинга и управления:
  мониторинг логов (SSE), управление контейнерами (start/stop/restart через Docker-API),
  редактор абонентов HSS (`userdb.py`).
- `srsran_configs/` — конфигурационные файлы srsRAN (enb.conf, epc.conf, rr.conf, sib.conf, ue.conf, user_db.csv и их .md-описания).
  - `srsran_configs/prod/` — эталонные конфиги по узлам (enb1..enb5, epc).
  - `srsran_configs/enb1..enb5/` — конфиги отдельных БС.
- `docs/` — техническая документация (ГОСТ) и описания.
- `srsenb/`, `srsepc/`, `srsue/`, `lib/`, `srsgnb/` — исходный код srsRAN.

## Файлы документации (куда смотреть за контекстом)

| Файл | Что содержит |
|------|--------------|
| `srs-readme.md` | 🔴 **АКТУАЛЬНО**: как поднять стенд (порядок скриптов по узлам, адреса, проверки, авто-перезапуск). Копия лежит в `~/srs-readme.md` на каждой малинке. |
| `docs/WALLSTAND_TECH_DESCRIPTION.md` (+ .pdf) | Техническое описание стенда в стиле ГОСТ (архитектура, блоки, питание, антенны). |
| `docs/STARTUP_SEQUENCE.md` | Последовательность запуска (⚠️ адреса устарели — сверять с `srs-readme.md`). |
| `TRACKING_RETURN_EPC.md` | Заметки/отслеживание задач (в т.ч. возврат EPC). |
| `srsran_configs/*.md` | Описания форматов конфигов (enb.conf.md, epc.conf.md, rr.conf.md, user_db.csv.md и др.). |
| `srsran-dashboard/README.md` | Документация дашборда. |
| `docker/README.md`, `test/README.md` | Сборка в Docker, тесты. |

## Типовые задачи и инструменты

- **Запуск/остановка узла** — см. `srs-readme.md`; скрипты в `experiment/`.
- **Контейнеры** запускаются с `--restart unless-stopped` (авто-возврат после подачи питания).
- **Проверка статуса**: `docker ps` на узле; логи через `docker logs -t <имя>`.
- **Диагностика БС**: `vcgencmd get_throttled` (троттлинг), `docker logs enb`, `uhd_find_devices`.
- **Передача между малинками** — через локальный ПК-посредник (scp) или по SSH-алиасам
  (`pi4`, `base5`, `pi5`) из `~/.ssh/config` инициатора.

## Известные ограничения / заметки

- **Меж-eNB хендовер** (S1) в этом форке НЕ работает end-to-end: EPC/MME
  (`srsepc/src/mme/s1ap.cc`) не обрабатывает `HandoverRequired`. Конфигурация
  соседства/измерений (A3) есть, но реальный хендовер требует доработки MME.
  Подробности — в памяти/заметках по проекту.
- **Питание БС**: слабый БП/USB-C кабель → троттлинг (`throttled` не 0x0) и
  нестабильность TX. Нужен качественный БП 5В/5А.
- **Первый старт eNB** после долгого простоя занимает ~3-5 мин (формирование
  FFT-плана в `~/srsran/fftw`); последующие старты — мгновенные.
- **Healthcheck `epc`** показывает "unhealthy" — это штатно (healthcheck под srsenb).

## Репозиторий / git

- Изменения коммитятся в `origin/master` (GitHub `OPKCP/srsRAN_4G`).
- Не добавлять временные/отладочные файлы (например, копии `.apk`, tar-образы,
  скрипты-помощники в `experiment/` без явной нужды) — держать рабочее дерево чистым.
