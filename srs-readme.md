# srsRAN 4G — запуск инфраструктуры стенда

Здесь описано, как поднять инфраструктуру стенда LTE (srsRAN 4G) **без участия
разработчика**: какие контейнеры должны быть запущены, в каком порядке и какими
скриптами. Все контейнеры настроены на авто-перезапуск (`--restart unless-stopped`):
при подаче питания малинки контейнер, работавший на момент выключения, поднимется сам.

> Docker-демон должен быть активен: `sudo systemctl start docker` (обычно уже автозапущен).
> SSH-псевдонимы в `~/.ssh/config`: `pi4` (ядро), `base5` (БС1), `pi5` (БС2).

---

## Состав стенда

| Узел | IP | Роль | SSH |
|------|----|------|-----|
| **pi4** | 192.168.1.27 | **Ядро сети EPC** (srsepc), веб-сервер, SIP (Asterisk), дашборд ядра | `ssh pi4` |
| **Base5** (eNB1) | 192.168.1.25 (wifi) | **Базовая станция №1** (srsenb) | `ssh base5` |
| **pi5-2** (eNB2) | 192.168.1.26 (wifi) | **Базовая станция №2** (srsenb) | `ssh pi5` |

Несущие частоты базовых станций:
- **eNB1** (Base5): **1815 МГц** (EARFCN 1300), pci=1, eNB id 0x19b
- **eNB2** (pi5-2): **1811 МГц** (EARFCN 1260), pci=5, eNB id 0x19f

Каналы разнесены (полоса 3 МГц + защитный интервал): 1815 → 1811 МГц.

---

## pi4 (ЯДРО + сервисы) — IP 192.168.1.27

Здесь размещены ядро сети и все сервисы ядра. Порядок запуска **важен**
(сервисы в namespace EPC зависят от запущенного контейнера `epc`):

1. **Ядро EPC** — обязательное (настраивает NAT/форвардинг для 10.0.0.0/24)
   ```
   bash ~/srsran/scripts/run_epc_pi5-2.sh
   ```
2. **Веб-сервер раздачи файлов** (namespace EPC, http://10.0.0.1:8080/)
   ```
   bash ~/srsran/scripts/run_webserver_pi5-2.sh
   ```
3. **Asterisk (SIP/голос)** (namespace EPC, 10.0.0.1:5060, абоненты 1001+)
   ```
   bash ~/srsran/asterisk/run_asterisk_pi5-2.sh
   ```
4. **Дашборд ядра** (мониторинг + управление + редактор абонентов HSS):
   `http://192.168.1.27:5001`
   ```
   cd ~/srsran/dashboard
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard-epc --mode epc \
     --logdir ~/srsran/logs --port 5001 --userdb ~/srsran/config_epc
   ```

**Проверка:**
```
docker ps   # должны быть: epc, webserve, asterisk, srsran-dashboard-epc
```

> Примечание: контейнер `epc` показывает healthcheck как "unhealthy" — это
> штатно (healthcheck проверяет srsenb, для srsepc не применим). Ядро при этом
> работает (SCTP S1-MME 36412, GTP-U 2152 на 192.168.1.27).

---

## Base5 (eNB1) — IP 192.168.1.25 (wifi), БС1

1. **БС1 (eNB1)** — базовая станция (mme_addr → 192.168.1.27)
   ```
   bash ~/srsran/scripts/run_enb_rpi.sh
   ```
2. **Дашборд** (мониторинг + управление БС1): `http://192.168.1.25:5000`
   ```
   cd ~/srsran-dashboard
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard --mode enb \
     --logdir ~/srsran/local/logs --port 5000
   ```

**Проверка:**
```
docker ps   # должны быть: enb, srsran-dashboard
```

---

## pi5-2 (eNB2) — IP 192.168.1.26 (wifi), БС2

Ядро с этого узла **снято** (перенесено на pi4). Здесь остаётся только БС2.

1. **БС2 (eNB2)** — базовая станция (mme_addr → 192.168.1.27)
   ```
   bash ~/scripts/run_enb2_pi5-2.sh
   ```
2. **Дашборд** (мониторинг + управление БС2): `http://192.168.1.26:5000`
   ```
   cd ~/srsran/dashboard
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard-enb2 --mode enb \
     --logdir ~/srsran/logs --port 5000
   ```

**Проверка:**
```
docker ps   # должны быть: enb2, srsran-dashboard-enb2
```

> На этом узле контейнеры ядра (`epc`, `asterisk`, `webserve`, `srsran-dashboard-epc`)
> остановлены и сохранены — при необходимости их можно поднять, но для текущей
> архитектуры они не нужны.

---

## Важные замечания

- **Настройка авто-перезапуска**: все скрипты запуска контейнеров содержат
  `--restart unless-stopped`. Контейнер поднимется сам при старте docker
  (например, после подачи питания), если он был запущен на момент выключения.
- **Если контейнер принудительно остановили** (`docker stop <имя>`), он НЕ
  перезапустится — поднимите заново скриптом выше.
- **Первый запуск eNB после долгого простоя** занимает ~3–5 мин (формирование
  FFT-плана). Это нормально — дождитесь в логе `eNodeB started` /
  `Setting frequency`. План сохраняется в `~/srsran/fftw` — последующие старты
  мгновенные.
- **Питание малинок**: при слабом БП/кабеле возможен троттлинг (`throttled`).
  На БС1 использовался USB-C кабель на 5А (иначе under-voltage). Проверка:
  `vcgencmd get_throttled` (0x0 = ок).
- **NAT для абонентов (10.0.0.0/24)** настраивается скриптом `run_epc_pi5-2.sh`
  на pi4 (MASQUERADE + FORWARD). При некорректном состоянии сети повторите
  запуск EPC.

---

## Полный стек после запуска

- Ядро (EPC) + сервисы → 192.168.1.27
- БС1 → 192.168.1.25
- БС2 → 192.168.1.26
- Дашборды: ядро 192.168.1.27:5001, БС1 192.168.1.25:5000, БС2 192.168.1.26:5000
- Веб-раздача: 10.0.0.1:8080 (для телефонов)
- SIP/голос: 10.0.0.1:5060 (для телефонов, абоненты 1001–1004)

Обе базовые станции работают на разных частотах (1815 и 1811 МГц) — каналы
разнесены.
