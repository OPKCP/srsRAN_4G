# srsRAN 4G — запуск инфраструктуры стенда

Здесь описано, как поднять инфраструктуру стенда LTE (srsRAN 4G) **без участия
разработчика**: какие контейнеры должны быть запущены, в каком порядке и какими
скриптами. Все контейнеры настроены на авто-перезапуск (`--restart unless-stopped`):
при подаче питания малинки контейнер, работавший на момент выключения, поднимется сам.

> Docker-демон должен быть активен: `sudo systemctl start docker` (обычно уже автозапущен).

---

## Состав стенда

| Узел | IP | Роль |
|------|----|------|
| **pi5-2** | 192.168.1.16 | Ядро сети EPC (srsepc), БС2 (eNB2), веб-сервер, дашборды |
| **Base5** (eNB1) | 192.168.1.25 (wifi) | Базовая станция №1 (srsenb) |

Несущие частоты базовых станций:
- **eNB1** (Base5): **1815 МГц** (EARFCN 1300), pci=1
- **eNB2** (pi5-2): **1811 МГц** (EARFCN 1260), pci=5

---

## pi5-2 (ядро + БС2 + сервисы) — IP 192.168.1.16

Порядок запуска **важен** (сервисы в namespace EPC зависят от запущенного `epc`):

1. **Ядро EPC** — обязательное
   ```
   bash ~/srsran/scripts/run_epc_pi5-2.sh
   ```
2. **БС2 (eNB2)** — базовая станция
   ```
   bash ~/scripts/run_enb2_pi5-2.sh
   ```
3. **Веб-сервер раздачи файлов** (namespace EPC, http://10.0.0.1:8080/)
   ```
   bash ~/srsran/scripts/run_webserver_pi5-2.sh
   ```
4. **Asterisk (SIP/голос)** (namespace EPC, 10.0.0.1:5060, абоненты 1001+)
   ```
   bash ~/srsran/asterisk/run_asterisk_pi5-2.sh
   ```
5. **Дашборды** (мониторинг + управление):
   - ядро: `http://192.168.1.16:5001`
   - БС2 (eNB2): `http://192.168.1.16:5000`
   ```
   cd ~/srsran/dashboard
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard-epc  --mode epc --logdir ~/srsran/logs --port 5001 --userdb ~/srsran/config_epc
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard-enb2 --mode enb --logdir ~/srsran/logs --port 5000
   ```

**Проверка:**
```
docker ps   # должны быть: epc, enb2, webserve, asterisk, srsran-dashboard-epc, srsran-dashboard-enb2
```

> Примечание: `epc` показывает контейнерный healthcheck как "unhealthy" — это
> штатно (healthcheck проверяет srsenb, для srsepc не применим).

---

## Base5 (eNB1) — IP 192.168.1.25 (wifi)

1. **БС1 (eNB1)** — базовая станция
   ```
   bash ~/srsran/scripts/run_enb_rpi.sh
   ```
2. **Дашборд** (мониторинг + управление БС1): `http://192.168.1.25:5000`
   ```
   cd ~/srsran-dashboard
   bash scripts/run_dashboard_rpi.sh --name srsran-dashboard --mode enb --logdir ~/srsran/local/logs --port 5000
   ```

**Проверка:**
```
docker ps   # должны быть: enb, srsran-dashboard
```

---

## Важные замечания

- **Настройка авто-перезапуска**: все скрипты запуска контейнеров содержат
  `--restart unless-stopped`. Это значит контейнер поднимется сам при старте
  docker (напр. после подачи питания), если он был запущен на момент выключения.
- **Если контейнер принудительно остановили** (`docker stop <имя>`), он НЕ
  перезапустится автоматически — нужно поднять заново скриптом выше.
- **Первый запуск eNB после долгого простоя** занимает ~3–5 мин (формирование
  FFT-плана). Это нормально — дождитесь в логе `eNodeB started` /
  `Setting frequency`. План сохраняется в `~/srsran/fftw` (на Base5) —
  последующие старты мгновенные.
- **Питание eNB1 (Base5)**: малинка может показывать `throttled` при слабом
  USB-C кабеле. Используйте кабель на 5А.
- **NAT для абонентов (10.0.0.0/24)** настраивается скриптом `run_epc_pi5-2.sh`
  (MASQUERADE + FORWARD). При некорректном состоянии сети повторите запуск EPC.

---

## Полный стек после запуска

- Ядро + БС2 + сервисы → 192.168.1.16
- БС1 → 192.168.1.25
- Дашборды: ядро 192.168.1.16:5001, БС2 192.168.1.16:5000, БС1 192.168.1.25:5000
- Веб-раздача: 10.0.0.1:8080 (для телефонов)
- SIP/голос: 10.0.0.1:5060 (для телефонов, абоненты 1001–1004)

Обе базовые станции работают на разных частотах (1815 и 1811 МГц) — каналы
разнесены.
