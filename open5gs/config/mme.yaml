# Open5GS — 4G EPC конфигурация MME (шаблон)
# Целевой стенд: ядро на pi5-2 (192.168.1.26).
#
# ВАЖНО: значения PLMN (mcc/mnc), TAC, адресов уточняются при развёртывании.
#   - В `srsran_configs/epc.conf`: mcc=250, mnc=01, tac=0x0007, apn=internet.mts.ru
#   - В `srsran_configs/prod/readme.md`: mcc=250, mnc=63, apn=IS2026.net
# Фактическая рабочая конфигурация — на малинках; сверять перед запуском.
#
# Источник: https://github.com/open5gs/open5gs/blob/main/configs/open5gs/mme.yaml.in

logger:
  file:
    path: /var/log/open5gs/mme.log
  level: info

global:
  max:
    ue: 1024

mme:
  freeDiameter: /etc/freeDiameter/mme.conf
  s1ap:
    server:
      - address: 192.168.1.26          # S1-MME bind (SCTP 36412) — pi5-2 (наружу, к БС)
  gtpc:
    server:
      - address: 127.0.0.2             # GTP-C S11 bind (внутр.) — разные loopback, чтобы не конфликтовать с SGWC/SMF на 2123
    client:
      sgwc:
        - address: 127.0.0.3           # SGWC (S11) на своём loopback-адресе
      smf:
        - address: 127.0.0.4           # SMF/PGW-C (S5c) на своём loopback-адресе
  metrics:
    server:
      - address: 127.0.0.2
        port: 9090
  gummei:
    - plmn_id:
        mcc: 250
        mnc: 63
      mme_gid: 2
      mme_code: 1
  tai:
    - plmn_id:
        mcc: 250
        mnc: 63
      tac: 7                              # TAC 0x0007
  security:
    integrity_order: [ EIA2, EIA1, EIA0 ]
    ciphering_order: [ EEA0, EEA1, EEA2 ]
  network_name:
    full: IS2026
    short: IS2026
  mme_name: open5gs-mme0
